// Destructuring lets you extract values from arrays or objects into variable in one line

let username = {
    name : "navicodes",
    age : 22,
    country : "India"
}

let {name, age, country} = username;

console.log(name);
console.log(age);